{% extends 'base.html.twig' %}

{% block title %}Reservation{% endblock %}

{% block body %}
    <h1>Reservation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reservation.id }}</td>
            </tr>
            <tr>
                <th>DateHeureDebut</th>
                <td>{{ reservation.DateHeureDebut ? reservation.DateHeureDebut|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>DateHeureFin</th>
                <td>{{ reservation.DateHeureFin ? reservation.DateHeureFin|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>TitreEvenements</th>
                <td>{{ reservation.TitreEvenements }}</td>
            </tr>
        </tbody>
    </table>

    

   <a href="{{ path('reservation_edit', {'id': reservation.id}) }}">Editer</a>

    {{ include('reservation/_delete_form.html.twig') }}
    <div class="container mt-4">
    <a href="{{ path('reservation_index') }}">Retour a la liste</a>
    </div>
{% endblock %}
